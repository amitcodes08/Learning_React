// import { render, screen } from "@testing-library/react";
// import RestaurantMenu from "../RestaurantMenu";
// import "@testing-library/jest-dom";

// it("RestaurantMenu Component", () => {
//     const { getByText } = render(<RestaurantMenu />);
//     const menuElement = getByText("Menu");
//     expect(menuElement).toBeInTheDocument();
// });

// The above test is commented out for now. It can be enabled and modified once the RestaurantMenu component is ready for testing.